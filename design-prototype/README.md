# RivaStock — Ink Cobalt design prototype

Static, design-only prototype. It does not connect to the real application, a backend, or persistent storage.

## Run locally

From this directory:

```powershell
python -m http.server 4173
```

Open <http://localhost:4173/#inicio>.

You can also open `index.html` directly, although a local server is recommended.
